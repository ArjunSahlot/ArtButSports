# Wireframes

The provided build brief described two sketches but the repository did not contain image files in `docs/wireframes/` when implementation began.

Implemented mapping:

- Query sketch: centered hero, dashed upload/drop/paste target, top-level weight bar, settings modal for sub-weights, and infinite masonry gallery.
- Visualize sketch: title plus one source section each for embeddings, composition, color, and pose, with before-to-after panels and a "Try it with your own" modal that calls the real backend step.

